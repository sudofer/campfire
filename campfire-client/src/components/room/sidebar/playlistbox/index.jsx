import React from "react";

export default function PlaylistBox() {
  return (
    <>
      {/* <ul>
        {results.map((result) => {
          return (
            <SearchItem
            // addPlayListItem={addPlayListItem}
            // key={result.id}
            // result={result}
            // url={url}
            />
          );
        })}
      </ul> */}
    </>
  );
}
